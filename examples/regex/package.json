{
  "name": "playground",
  "version": "1.0.0",
  "description": "switch files to test js behaviour",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "babel-node --presets es2015 regex.js"
  },
  "author": "milan keca",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.0.0"
  },
  "dependencies": {
    "readline-sync": "^1.4.5"
  }
}
